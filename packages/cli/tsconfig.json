{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        // Targeting node >= 8
        "target": "es2017",
        "module": "commonjs",
        "types": [
            // Don't include mocha here
            "jest",
            "node",
        ]
    },
    "exclude": ["tests/extract/integration_tests/typescript/**/*.tsx"],
}
